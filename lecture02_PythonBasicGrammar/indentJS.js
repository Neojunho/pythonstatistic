var sumResult = plusCalculator(5, 10);
console.log(sumResult);
function plusCalculator(x, y) {
    return x + y;
}

var sumResult = plusCalculator(5, 10);
console.log(sumResult);
function plusCalculator(x, y) {
return x + y;
}